// Moved to the SDK (S10a): sdk/recover.js is the canonical corruption recovery. This
// shim keeps existing imports working (tools/e2e-corrupt-test.mjs imports this path).
export * from '@aliran/player-sdk/recover.js'
